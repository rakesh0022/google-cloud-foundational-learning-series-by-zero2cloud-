# Day 2 — Summary

## 🎯 Topic: Deploy Your First Web App on Google Cloud (Compute Engine)

### What viewers should learn:
- What Compute Engine (VM) actually is.
- Why VMs are the foundation of GCP.
- How to create a VM → install NGINX → attach a persistent disk.

### Key Talking Points:
- VM = a computer inside Google’s data center (CPU/RAM/OS of your choice).
- You don’t manage physical hardware → only pay for usage.
- Step-by-step → Create VM → Choose machine type → Configure OS + firewall.
- Demo: SSH → apt update → install nginx.
- Persistent Disk:
  - Like attaching an external hard disk.
  - Data stays safe even if VM is deleted.

### Micro Summary for Pace:
- What a VM is
- Why Compute Engine matters
- Deploy NGINX
- Attach Persistent Disk
